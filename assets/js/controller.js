'use strict';

const $ = require('jquery');

const activateSearch = () => {
  // checks active tab in listener
  // on enter listener
};

const switchTabs = tab => {
  // search: clear
  // lists: grab recent
};

