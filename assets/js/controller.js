'use strict';

const $ = require('jquery');

function activateSearch () {
  // checks active tab in listener
  // on enter listener
}

function switchTabs (tab) {
  // search: clear
  // lists: grab recent
}

